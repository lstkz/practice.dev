{
  "name": "projects",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "sideEffects": false,
  "scripts": {
    "tsc": "tsc --noEmit",
    "ts-node": "ts-node -T",
    "lint": "tslint -c tslint.json \"src/**/*.{ts,tsx}\"",
    "prettier": "prettier --write '{src,scripts}/*.{ts,tsx}'",
    "deploy": "ts-node -T deploy",
    "test": "yarn run tsc && yarn run lint"
  },
  "dependencies": {
    "@pvd/tools": "*"
  },
  "devDependencies": {},
  "keywords": [],
  "author": "",
  "license": "UNLICENSED"
}
