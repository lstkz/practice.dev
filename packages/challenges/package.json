{
  "name": "challenges",
  "version": "1.0.0",
  "description": "",
  "sideEffects": false,
  "scripts": {
    "tsc": "tsc --noEmit",
    "ts-node": "ts-node -T",
    "lint": "tslint -c tslint.json \"src/**/*.{ts,tsx}\"",
    "prettier": "prettier --write '{src,scripts}/*.{ts,tsx}'",
    "start": "webpack-dev-server --config webpack.demo.config",
    "deploy": "ts-node -T deploy",
    "test": "yarn run tsc && yarn run lint"
  },
  "dependencies": {
    "@pvd/tools": "*"
  },
  "devDependencies": {},
  "keywords": [],
  "author": "",
  "license": "UNLICENSED"
}
